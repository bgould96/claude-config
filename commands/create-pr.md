Diff this branch against main. Scan the diff for dangerous secrets. If any are found, create a plan to resolve them.

DO NOT create a PR if any dangerous secrets are hardcoded.

Once all dangerous secrets are resolved, create a PR using the gh cli
